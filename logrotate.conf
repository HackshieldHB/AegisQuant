# Log rotation suggestion for AegisQuant (copy to /etc/logrotate.d/aegisquant or use in cron)
# Rotate logs daily, keep 7, compress after rotation.

/path/to/AegisQuant/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
